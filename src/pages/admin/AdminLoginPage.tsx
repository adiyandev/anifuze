import {FormEvent,useState} from 'react';import {useNavigate} from 'react-router-dom';import {ShieldCheck,KeyRound,LockKeyhole} from 'lucide-react';import {useApp} from '../../contexts/AppContext';
export function AdminLoginPage(){const nav=useNavigate();const{setRole}=useApp();const[step,setStep]=useState<'credentials'|'2fa'>('credentials');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[code,setCode]=useState('');const[error,setError]=useState('');const submit=(e:FormEvent)=>{e.preventDefault();setError('');if(step==='credentials'){if(!email||password.length<8){setError('Enter valid admin credentials.');return}setStep('2fa');return}if(!/^\d{6}$/.test(code)){setError('Enter a valid 6-digit verification code.');return}setRole('owner');nav('/admin/dashboard')};return <div className="admin-login-page"><div className="admin-login-card"><div className="admin-login-brand"><span>✦</span><strong>AniFuze</strong><small>ADMIN PANEL</small></div>{step==='credentials'?<><div className="admin-login-icon"><LockKeyhole size={22}/></div><h1>Admin sign in</h1><p>Secure access to your AniFuze installation.</p><form onSubmit={submit}><label>Email<input type="email" autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} minLength={8} required/></label><button className="button" type="submit">Continue</button></form></>:<><div className="admin-login-icon"><ShieldCheck size={22}/></div><h1>Verify your identity</h1><p>Enter your 6-digit authenticator or email verification code.</p><form onSubmit={submit}><label>Verification code<input inputMode="numeric" autoComplete="one-time-code" value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,'').slice(0,6))} maxLength={6} required/></label><button className="button" type="submit">Verify & enter</button><button type="button" className="link-button" onClick={()=>setStep('credentials')}>Back</button></form><div className="admin-login-note"><KeyRound size={14}/> Recovery codes are supported by the production authentication flow.</div></>}{error&&<div className="notice error">{error}</div>}</div></div>}