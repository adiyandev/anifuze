import {createContext,useContext,useState,useEffect,type ReactNode} from 'react';
import type {Role,Settings} from '../types';
import {settingsService} from '../services/mockServices';
type Toast={message:string;kind?:'success'|'error'|'info'};
type AppState={role:Role;setRole:(r:Role)=>void;settings:Settings;refresh:()=>void;toast:(m:string,k?:Toast['kind'])=>void;permissions:string[];can:(permission:string)=>boolean;adminVerified:boolean;refreshAuth:()=>Promise<void>};
const Context=createContext<AppState>(null!);
export const useApp=()=>useContext(Context);
export function AppProvider({children}:{children:ReactNode}){const[role,setRoleState]=useState<Role>('public_user');const[permissions,setPermissions]=useState<string[]>([]);const[adminVerified,setAdminVerified]=useState(false);const[settings,setSettings]=useState(settingsService.get());const refreshAuth=async()=>{try{const me=await fetch('/api/auth/admin/me');if(!me.ok){setRoleState('public_user');setPermissions([]);setAdminVerified(false);return;}const md=await me.json();const verified=Boolean(md?.user?.twoFactorVerified)&&!md?.user?.mustSetup2fa;setAdminVerified(verified);if(!verified){setRoleState('public_user');setPermissions([]);return;}setRoleState(md.user.role);const p=await fetch('/api/auth/admin/permissions');if(p.ok){const pd=await p.json();setPermissions(Array.isArray(pd.permissions)?pd.permissions:[]);}else setPermissions([]);}catch{setRoleState('public_user');setPermissions([]);setAdminVerified(false);}};useEffect(()=>{refreshAuth();},[]);const[note,setNote]=useState<Toast|null>(null);const updateRole=(r:Role)=>setRoleState(r);const refresh=()=>setSettings(settingsService.get());const toast=(message:string,kind:Toast['kind']='success')=>{setNote({message,kind});window.setTimeout(()=>setNote(null),3000)};return <Context.Provider value={{role,setRole:updateRole,settings,refresh,toast,permissions,can:p=>permissions.includes(p),adminVerified,refreshAuth}}>{children}{note&&<div className={`toast ${note.kind}`}>{note.message}</div>}</Context.Provider>}