import test from 'node:test';import assert from 'node:assert/strict';
import {PROVIDER_SDK_VERSION,createProviderContext,interpolate,normalizeProviderResponse,validateProviderDefinition,validateProviderSdkManifest} from './providerSdk.js';
test('provider SDK creates and interpolates context',()=>{const c=createProviderContext({animeId:'a/b',episode:3});assert.equal(c.episodeId,'3');assert.equal(interpolate('/anime/{animeId}/episode/{episode}',c),'/anime/a%2Fb/episode/3');});
test('provider SDK validates definitions',()=>{assert.equal(validateProviderDefinition({type:'API',source:{endpoint:'/sources'}}),true);assert.equal(validateProviderDefinition({type:'Embed',urlTemplate:'https://example.test/{animeId}'}),true);assert.throws(()=>validateProviderDefinition({type:'API'}));});
test('provider SDK validates versioned operation manifest',()=>{const m=validateProviderSdkManifest({sdkVersion:PROVIDER_SDK_VERSION,operations:{search:{endpoint:'/search?q={query}'},getSources:{endpoint:'/sources/{animeId}/{episode}'}}});assert.equal(m.operations.search.endpoint,'/search?q={query}');assert.throws(()=>validateProviderSdkManifest({sdkVersion:2,operations:{}}));assert.throws(()=>validateProviderSdkManifest({sdkVersion:1,operations:{stream:{endpoint:'/stream'}}}));});
test('provider SDK normalizes common source response shapes',()=>{assert.deepEqual(normalizeProviderResponse({sources:[{file:'https://cdn.test/a.m3u8',quality:'1080p'}]}),[{url:'https://cdn.test/a.m3u8',type:'auto',quality:'1080p',language:''}]);});
