// Player sources removed – no embed providers
export const PLAYER_SOURCES = [];

export const getSourceUrl = (sourceId, type, id, season, ep, extraParams = {}, accentColor = null, subtitleLang = null) => {
  // No providers available, return empty string
  return '';
};

export const sourceSupportsProgress = (sourceId) => false;
export const sourceProgressViaFrames = (sourceId) => false;
